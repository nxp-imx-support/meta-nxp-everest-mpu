# Drop-in override that adds a hard dependency on the CG5317 firmware
# loader without modifying the upstream everest.service file. Survives
# package upgrades that replace everest.service.

[Unit]
After=cg5317-firmware-load.service
Requires=cg5317-firmware-load.service
